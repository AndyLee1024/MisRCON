/**
 * Name: notifications
 * Type: Redux Reducer
 * Description:
 */

const initialState = {};

export default function notifications(state = initialState, action) {
	switch (action.type) {
		default:
			return state;
	}
}
