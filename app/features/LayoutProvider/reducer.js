/**
 * Name: layoutProvider
 * Type: Redux Reducer
 * Description:
 */

import initialState from './state';

export default function layoutProvider(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}
