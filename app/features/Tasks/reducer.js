/**
 * Name: tasks
 * Type: Redux Reducer
 * Description:
 */

const initialState = {};

export default function tasks(state = initialState, action) {
	switch (action.type) {
		default:
			return state;
	}
}
